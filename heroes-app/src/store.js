import { createStore } from "vuex";

const state = {
  caption: "Vuex works!"
};

const getters = {};

const mutations = {};

const actions = {};

export default createStore({
  state,
  getters,
  mutations,
  actions,
  modules: {}
});
